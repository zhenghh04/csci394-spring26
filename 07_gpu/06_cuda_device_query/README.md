# CUDA device query

This Chapter 18 example checks that CUDA is installed and reports the first GPU.

Concepts:

- `cudaGetDeviceCount`
- `cudaGetDeviceProperties`
- quick sanity check before running larger CUDA examples

Build:

```bash
make
```

Run:

```bash
./app
```
